export class Layer {
  constructor(id, name) {
    this.id = id
    this.name = name
  }
  

}

export class Transition {
  constructor(id, from, to) {
    this.id = id
    this.from = from
    this.to = to
  }
}